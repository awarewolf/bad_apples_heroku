<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" rel="home" href="/movies" title="Bad Apples">
        <img id="brandname" alt="Bad Apples" src="/assets/bad_apples_text.png">
      <img id="brandlogo" src="https://upload.wikimedia.org/wikipedia/commons/d/dc/Apple_with_worm_dan_gerh_01.svg"></a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-brand-centered">
      <ul class="nav navbar-nav">
        <!--         <%= navlink("Movies",Movie) %>
        -->        <% if admin? %>
        <%= navlink("Users",[:admin, User]) %>
        <% end %>
      </ul>
      <form class="navbar-form navbar-left" role="search">
        <%= form_tag movies_url, class: 'form-inline', method: :get do %>
        <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
        <div class="form-group ">
          <label class="sr-only">Search</label>
          <%= text_field_tag( :search, nil, {class: 'form-control', placeholder: "Title/Director/Plot"}) %>
        </div>
        <%= submit_tag "Search", class: 'btn btn-primary' %>
        <% end %>
        <!-- <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-default">Submit</button> -->
        </form>
        <% if current_user %>
        <div class='navbar-text navbar-right' style="margin-right:10px;">
          <strong>Welcome <%= current_user.full_name %><strong>
        </div>
        <div class='navbar-form navbar-right'>
          <%= link_to "Log out", session_path("current"), method: :delete, class: "btn btn-success" %>
        </div>
        <% else %>
        <div class='navbar-form navbar-right'>
          <%= form_tag sessions_path do %>
          <div class="form-group">
            <%= label_tag 'email', nil, class: "sr-only" %>
            <%= text_field_tag 'email', nil, class: "form-control", placeholder: "email" %>
          </div>
          <div class="form-group">
            <%= label_tag 'password', nil, class: "sr-only" %>
            <%= password_field_tag 'password', nil, class: "form-control", placeholder: "password" %>
          </div>
          <div class="form-group">
            <%= submit_tag "Sign In", class: "btn btn-success" %>
          </div>
          <div class="form-group">
            <%= link_to "Sign Up", new_user_path, class: "navbar-signup" %>
          </div>
          <% end %>
        </div>
        <% end %>
        </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>